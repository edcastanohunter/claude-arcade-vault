// Generado por el MCP de Supabase (generate_typescript_types) en la Fase 2
// del spec 04, una vez exista el esquema. Este placeholder solo evita que
// el build falle antes de esa fase.
export type Database = Record<string, never>;
